import React from 'react'

const card=(props)=> {
    console.log(props.data);
    return (
        <div className="cardContainer">
            {data.ma}
            
        </div>
    )
}

export default card